{
  "name": "cerebrum",
  "version": "5.0.0",
  "description": "A free scientific literature search engine — ask a research question, get a cited answer synthesized from open scholarly databases.",
  "private": true,
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/Vaticay/cerebrum"
  },
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --proxy 5173 -- vite",
    "vite": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "vite build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8",
    "wrangler": "^3.78.0"
  }
}
